"""Everything that happens after a pose exists: scoring, comparison and presentation.

Nothing here runs an engine. It reads result rows and poses and produces numbers
(metrics, rmsd, redocking), contacts (interactions, diagram) or tables (tables, dataset).
"""
